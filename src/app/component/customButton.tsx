import RippleButton from "./RippleButton";

const customButton = () => {
    return(
        <div>
            <RippleButton>
                Hi, LolHP
            </RippleButton>
        </div>
    )
};

export default customButton;